# KISS — Keep Insurance Super Simple

**Insurance Policy Analysis Service**

Upload your homeowner's insurance policy PDF. KISS extracts coverage details, identifies gaps, and delivers a plain-English report in 5–10 minutes.

---

## What is KISS?

A policy analysis service that takes a homeowner's insurance policy PDF, extracts structured data with Claude 3.5 Sonnet, and produces a plain-English advisory report. Optionally, when a partner (insurance broker or property attorney) refers the homeowner, it produces a *second* report tailored to that partner's POV (broker advisory or pre-claim analysis) and emails both copies — homeowner gets the consumer report directly, partner gets both reports with the homeowner CC'd.

**No CRM lock-in.** Partners use whatever CRM they already use; KISS just emails them the report.

## Quick Start

```bash
npm install
npm run dev  # Start on http://localhost:3000
```

See `PHASE_C_NEXT_STEPS.md` for Vercel deployment.

---

## Architecture (v2 — Canonical)

| Component | Technology |
|-----------|------------|
| **Frontend** | React 18 + Vite 5 + React Router |
| **Hosting** | Vercel (kiss.optimizinggroup.com) |
| **Database** | Supabase (PostgreSQL + Auth + Storage) |
| **Orchestration** | Make.com webhook scenario |
| **LLM** | Claude 3.5 Sonnet (35% cheaper than GPT-4o) |
| **PDF Extraction** | PDF.co |
| **Email** | Resend (verified domain) |
| PDF text extraction | Raw bytes into GPT-4o | **PDF.co** → text → GPT-4o |
| Multi-tenancy | Implicit via `tenant_persona` field | **Explicit `tenants` table** with slug + partner_code |
| Make scenario | ID `4581471`, 18 modules, never executed | New blueprint, 16 modules, ready to import |
| Status | **Pause on cutover** (don't delete — rollback) | Code complete, not yet deployed |

**Decision rationale (from v2 handoff doc):** every target partner already has a CRM (Salesforce, AMS360, Clio, MyCase, HubSpot…) and won't migrate or maintain a parallel one in GHL. v2 strips GHL out so partners use their own CRM and KISS just emails reports.

## Where we stand

- **v2 code: complete.** All five files generated by a prior Claude session on April 12 (with iterations to v2 on April 30):
  - `code/supabase/schema.sql` — 5 tables, RLS policies, indexes, triggers, seed data
  - `code/frontend/KissIntakeForm.jsx` — React component, handles branded slug + generic+code routes
  - `code/make/blueprint_v2.json` — 16-module Make scenario blueprint
  - `code/emails/email_homeowner.html` — homeowner email template
  - `code/emails/email_partner.html` — partner email template
- **v2 deploy: 0%.** No Supabase project. No Resend account. No PDF.co account. No Vercel. No GitHub repo. No DNS pointing.
- **v1 (in Make): live but never executed.** Will be paused on v2 cutover, kept for rollback.

For the full state, read `00-current-status.md`. For the deployment punch list, read `01-next-steps.md`.

## Folder map

```
KISS/
├── README.md                          ← you are here
├── 00-current-status.md               ← what's built, what's not
├── 01-next-steps.md                   ← Phase A → D deployment checklist
│
├── docs/                              ← v2 documentation
│   ├── KISS_v2_Technical_Handoff.docx ← THE master spec from prior Claude session
│   ├── KISS_v2_Technical_Handoff.md   ← extracted readable version of same
│   └── Updated_Law_Firm_Intake_Workflow.docx ← companion content for attorney persona
│
├── code/                              ← v2 source — drop-in deploy
│   ├── frontend/
│   │   └── KissIntakeForm.jsx         ← React component (Vite + Router)
│   ├── supabase/
│   │   └── schema.sql                 ← run in Supabase SQL Editor
│   ├── make/
│   │   └── blueprint_v2.json          ← import via Make → New Scenario → Import Blueprint
│   ├── emails/
│   │   ├── email_homeowner.html       ← reference template (used inline in Make module 14)
│   │   └── email_partner.html         ← reference template (used inline in Make module 15)
│   └── KISS_v2_Complete.zip           ← original zip from prior Claude session
│
├── marketing/                         ← attorney-vertical product + compliance
│   ├── attorney-personas.md           ← PI/auto + Property + Phase 2 broker definition
│   ├── florida-bar-compliance.md      ← FL Bar Chapter 4-7 reference + checklist
│   └── nurture-sequences/             ← 5-email sequences over 90 days
│       ├── pi-auto-attorney-sequence.md
│       └── property-attorney-sequence.md
│
├── sales-page/                        ← the page you push attorneys to
│   └── kiss-for-attorneys.html        ← live mockup with ROI calculator
│
└── _v1-historical/                    ← v1 stuff, kept for rollback / archaeology only
    ├── docs/                          ← original v1 PDFs/docx
    ├── make-prompts/                  ← v1 GPT prompts as separate files
    ├── ghl/                           ← v1 GHL sub-account config
    ├── scenario-blueprint.json        ← v1 Make blueprint (current scenario 4581471)
    ├── scenario-blueprint-info.md
    └── webhook-payload-spec.md
```

## Stack

| Layer | Tool | Status |
|---|---|---|
| Frontend | React + Vite + React Router on **Vercel** | Code ready, not deployed |
| Domain | `kiss.optimizinggroup.com` | DNS not pointed |
| Storage / DB / Auth | **Supabase** (us-east, project name `kiss-prod`) | Project not created |
| Storage bucket | Supabase Storage `policies` (private, 25 MB, app/pdf only) | Not created |
| Orchestration | **Make.com** (org 5587731, team 1524441, us2) | v2 blueprint not imported |
| LLM | **OpenAI gpt-4o** (existing Make connection IMTCONN 6008119) | Reuse from v1 |
| PDF text extraction | **PDF.co** | Account not created |
| Email | **Resend** (domain verified at `kiss.optimizinggroup.com`) | Not created |
| Source control | GitHub (repo `kiss-frontend` under Optimizing Group org) | Not created |

## Pricing model (per Technical Handoff)

| Phase | Audience | Price |
|---|---|---|
| Phase 1 | Direct-to-consumer | $4.99 one-time |
| Phase 2 | White-label brokers/attorneys | $500–$1,000 setup + $97/month per partner |
| Cost per submission | All routes | $0.10–$0.50 (OpenAI tokens dominate) |

> Note: the v2 handoff doesn't explicitly bake in the $4.99 Stripe layer — v2's "consumer" is the default tenant in the tenants table, intake is the same form, no payment gate. If you still want the $4.99 paywall on the consumer side, you'll add a Stripe step in front of the form. Worth a discussion before deploy.

## Three intake routes (all hit the same downstream pipeline)

1. **Branded** — `kiss.optimizinggroup.com/{slug}` → form pre-branded with partner's logo/colors
2. **Generic + code** — `kiss.optimizinggroup.com/start` → enter partner code, or leave blank for default consumer tenant
3. **Email forwarding** — `policies@kiss.optimizinggroup.com` → forward partner email; Make resolves tenant by sender. *(Phase 1.5 — not built yet)*

## Key IDs (v2)

| What | Value |
|---|---|
| Document owner | Keith Kravitz · keith@optimizinggroup.com |
| Operating entity | Optimizing Group LLC |
| Make org / team | 5587731 / 1524441 (us2.make.com) |
| Make AI dev email | AIdeveloper@optimizinggroup.com |
| OpenAI Make connection | IMTCONN 6008119 (reuse from v1) |
| v1 Make scenario (legacy) | 4581471 — **PAUSE on v2 cutover, do not delete** |
| Domain | kiss.optimizinggroup.com |
| Supabase project name | `kiss-prod` (not yet created) |

## When in doubt — quick reference

- "What's the next concrete thing to do?" → `01-next-steps.md` Phase A.
- "Is the Make scenario actually built?" → v1 yes (paused on cutover); v2 no (blueprint is in `code/make/blueprint_v2.json`, not yet imported).
- "Why isn't this just one of the architectures?" → because the prior session re-architected from v1 → v2 to remove GHL. v1 is kept for rollback only.
- "What's the Supabase schema?" → `code/supabase/schema.sql` — 5 tables: tenants, submissions, reports, email_events, tenant_users.
- "What's the architecture flow?" → `docs/KISS_v2_Technical_Handoff.md` § 3.1 has the one-paragraph flow diagram.
- "What needs to happen before going live?" → `01-next-steps.md` Phase D (end-to-end test) + `docs/KISS_v2_Technical_Handoff.md` § 13 (known gaps & risks).
# KISS - Ready for deployment
